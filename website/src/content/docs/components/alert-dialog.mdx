---
title: Alert Dialog

---
import ComponentDemo from '@components/component-demo.astro';
import { Code } from '@astrojs/starlight/components';
import AlertDialog from '@components/demos/alert-dialog.astro';
import AlertDialogCode from '@components/demos/alert-dialog.astro?raw';
import APIReference from "@components/api-reference.astro";
import { alertDialogSchema } from "@api-reference/alert-dialog.api.ts";

The **Alert Dialog** component is a modal dialog designed for critical messages that require user attention. It’s perfect for displaying alerts, confirmations, or actions that users need to acknowledge before proceeding, ensuring they focus on important information in a clear, accessible way.

<ComponentDemo label="Alert dialog demo">
    <AlertDialog slot="preview" />
    <Code 
        slot="code" 
        lang="html" 
        code={AlertDialogCode} 
    />
</ComponentDemo>

## Structure

<Code lang="html" code={`
<div x-data="alertDialog">
    <button x-ref="trigger"></button>
    <template x-teleport="body">
        <div x-ref="wrapper" x-bind="wrapperProps">
            <div x-ref="backdrop" x-bind="backdropProps"></div>
            <div x-ref="content" x-bind="contentProps">
                <h2 x-ref="title"></h2>
                <p x-ref="description"></p>
                <button x-bind="closeProps"></button>
                <button @click="closeDialog();"></button>
            </div>
        </div>
    </template>
</div>
`} />

## API Reference

<APIReference schema={alertDialogSchema} />